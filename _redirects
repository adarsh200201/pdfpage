# API proxy rules - Route all /api/* requests to Render backend (rewrite, not redirect)
/api/* https://pdfpage-backend.onrender.com/api/:splat 200!

# SPA fallback for all other routes
/* /index.html 200
